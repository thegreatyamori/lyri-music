/**
 * The toolbar popup. A second presentation of the same lyrics the Overlay
 * shows; it exists for when the in-page panel is hidden.
 */
export function Popup() {
  return <main class="popup">LyriMusic</main>;
}
