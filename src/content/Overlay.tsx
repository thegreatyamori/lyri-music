/**
 * The in-page panel.
 *
 * For now it only proves the content script mounts and the shadow root is
 * isolated. The track, the lookup and the clock arrive in later phases.
 */
export function Overlay() {
  return <div class="lyrimusic-panel">LyriMusic</div>;
}
