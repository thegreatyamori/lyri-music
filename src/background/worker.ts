/**
 * The background module.
 *
 * Deliberately thin: it is an adapter from `chrome.runtime.onMessage` to the
 * Lookup. Nothing about provider order, caching or fan-out belongs here.
 *
 * The message surface is defined in a later phase.
 */

export {};
